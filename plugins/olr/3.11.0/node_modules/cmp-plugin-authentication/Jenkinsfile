banzaiNpmPipeline() {

  buildFork = true

  npm = [
    release: false,
    publish: true,
    artifact: false,
    scripts: [
      build: 'npm run build',
      test: 'npm run test'
    ]
  ]

  artifactStore = 'ARTIFACTORY'

  sonar = [
    exclusions:  'tests/**, node_modules/**, coverage/**',
    sources: 'src',
    'typescript.lcov.reportPaths': 'coverage/lcov.info'
  ]

  enableCheckmarx = true
  checkmarx = [
    checkmarxAdditionalPatterns: "**/*.ts, **/*.json, !**/package-lock.json, !**/coverage/, !**/tests/, !**/.nyc_output/",
    scanConfig: [
      teamPath: 'CxServer\\SP\\Cengage Learning\\IAM',
      osaEnabled: false
    ]
  ]
}
