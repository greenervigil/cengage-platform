banzaiNpmPipeline() {

  buildFork = true

  npm = [
    release: false,
    publish: true,
    artifact: false,
    scripts: [
      build: 'npm run build',
      test: 'npm run test'
    ]
  ]

  artifactStore = 'ARTIFACTORY'

  sonar = [
    exclusions:  'tests/**, node_modules/**, coverage/**',
    sources: 'src',
    'typescript.lcov.reportPaths': 'coverage/lcov.info'
  ]
}
